import 'dart:ui';

import 'package:flutter/material.dart';

class ColorContents{
  static final Color? greenColor = Color.lerp(Colors.green.shade900, Colors.green.shade500, 0.7);
  static final Color? orangeColor = Color.lerp(Colors.deepOrange, Colors.orange, 0.6);
}